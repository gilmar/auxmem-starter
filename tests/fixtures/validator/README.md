# Validator test fixtures

Validator adversarial cases live in `tests/test_validator.py` and use a
fresh scaffolded corpus per test (`validator_corpus` fixture). Notes are written
inline so each case is self-contained and reproducible.
